// Package arch hosts the architecture contract test C-ARCH-01 (BUILD.md 4.5,
// 7.2). It has no production code; it exists so the dependency-rule and
// go-ladybug-isolation checks run as part of the default `go test ./...`.
package arch

== Gc-carrier  invariant carrier reconciliation ==
  note   relational layers not opted in: policy, integrity, isolation
  checked: 18 invariants declared, 16 preserves references, 12 carried by preserves, 1 carried by a machine unit, 5 waived with reason
  ok
== G2-c4  Architecture Contract ==
  note   baseline: no rules; the ratchet holds no tolerated debt (mechanism unused)
  note   assert: no dependency assertions; negative reachability claims live only in prose (mechanism unused)
  checked: 7 boundaries, 2 externals, 13 allow rules, 21 transitive pairs, 2 deny rules, 6 boundaries with exposes, 12 dsl elements, 7 boundaries bound to dsl, 2 mitigation rows, 4 dependencies with mitigation rows
  ok
== G3-machine  machines + oracle ==
  warn   Portfolio.machine.json: liveness for reverted rests on an UNPROVEN _exhaustive claim (guards are erased in the TLA+ model, so TLC cannot check it); verify the guard set is provably total, or add an unguarded fallback branch so the liveness proof becomes sound
  note   Portfolio.machine.json: state reverted always-list is fully guarded; liveness rests on the declared exhaustiveness: prior is set by every setPending action to the review stage the operation departed from; its codomain is exactly the four PortfolioStatus values, so one guarded branch per stage is total
  checked: 3 machines, 33 transitions, 3 oracles fresh, 31 named units covered, 1 states relying on unproven _exhaustive liveness
== Gx-trace  cross-layer traceability ==
  warn   invariant 'candidate-deduped' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'candidate-from-top-30' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'holding-weight-nonneg' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'holding-weights-sum-full' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'index-top-30' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'run-terminal-absorbing' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'ticker-unique' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  note   Portfolio: 3 operational-overlay states (committing, commitRetry, reverted)
  note   RecommendationRun: 1 operational-overlay states (collectRetry)
  checked: 6 entities, 1 operational machines, 2 lifecycle machines traced, 2 lifecycle entities with machines, 3 placement rows with machines, 5 placement rows waived, 3 build shards scanned, 1 build mode declared, 1 toolchain section present, 1 state-migration section present, 7 invariants attested only (prose), 6 invariants enforced, 6 invariants unit-backed (guard/action/actor), 5 invariants waived with reason
== Gb-plan  build plan structure ==
  checked: 3 plans, 6 milestones, 6 DoD-bearing milestones, 2 skeleton waivers, 3 skeleton citations, skeleton citation skipped (skeleton waived), skeleton citation skipped (skeleton waived)
  ok

0 blocking (ERROR/DRIFT) finding(s)

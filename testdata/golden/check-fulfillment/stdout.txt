== Gi-integrity structural relational model ==
  checked: 3 entities, 2 relationships, 1 unique keys, 2 invariants carried, 3 solver commands generated, 1 committed artifacts fresh (byte-identical)
  ok
== Gc-carrier  invariant carrier reconciliation ==
  note   relational layers not opted in: policy, isolation
  checked: 25 invariants declared, 29 preserves references, 20 carried by preserves, 5 carried by a machine unit
  ok
== G2-c4  Architecture Contract ==
  note   baseline: no rules; the ratchet holds no tolerated debt (mechanism unused)
  note   assert: no dependency assertions; negative reachability claims live only in prose (mechanism unused)
  checked: 5 boundaries, 4 allow rules, 4 transitive pairs, 3 deny rules, 1 boundaries with exposes, 19 dsl elements, 20 drawn relationships, 20 drawn edges outside the contract vocabulary, 5 boundaries bound to dsl, 5 mitigation rows, 7 dependencies with mitigation rows, 3 nfr topics recorded, 1 event tables with sources, 48 event-contract cells answered, 16 event-contract participants resolved, 4 edges contracted, 8 event contracts
  ok
== G3-machine  machines + oracle ==
  warn   FulfillmentSaga.machine.json: counter 'retries' has an increment and an exhaustion guard but no reset action on any edge; if a re-issued entry is promised a fresh budget, wire the reset on the leg-entry edges, and if one budget per instance is the design, say so with its proof in _counters
  warn   Order.machine.json: counter 'retries' has an increment and an exhaustion guard but no reset action on any edge; if a re-issued entry is promised a fresh budget, wire the reset on the leg-entry edges, and if one budget per instance is the design, say so with its proof in _counters
  warn   Order.machine.json: liveness for rolledBack rests on an UNPROVEN _exhaustive claim (guards are erased in the TLA+ model, so TLC cannot check it); verify the guard set is provably total, or add an unguarded fallback branch so the liveness proof becomes sound
  warn   OutboxMessage.machine.json: counter 'retries' has an increment and an exhaustion guard but no reset action on any edge; if a re-issued entry is promised a fresh budget, wire the reset on the leg-entry edges, and if one budget per instance is the design, say so with its proof in _counters
  warn   OutboxMessage.machine.json: liveness for rolledBack rests on an UNPROVEN _exhaustive claim (guards are erased in the TLA+ model, so TLC cannot check it); verify the guard set is provably total, or add an unguarded fallback branch so the liveness proof becomes sound
  warn   Payment.machine.json: counter 'gatewayRetries' has an increment and an exhaustion guard but no reset action on any edge; if a re-issued entry is promised a fresh budget, wire the reset on the leg-entry edges, and if one budget per instance is the design, say so with its proof in _counters
  warn   Payment.machine.json: counter 'retries' has an increment and an exhaustion guard but no reset action on any edge; if a re-issued entry is promised a fresh budget, wire the reset on the leg-entry edges, and if one budget per instance is the design, say so with its proof in _counters
  warn   Payment.machine.json: liveness for gatewayResume, rolledBack rests on an UNPROVEN _exhaustive claim (guards are erased in the TLA+ model, so TLC cannot check it); verify the guard set is provably total, or add an unguarded fallback branch so the liveness proof becomes sound
  warn   Reservation.machine.json: counter 'retries' has an increment and an exhaustion guard but no reset action on any edge; if a re-issued entry is promised a fresh budget, wire the reset on the leg-entry edges, and if one budget per instance is the design, say so with its proof in _counters
  warn   Reservation.machine.json: liveness for rolledBack rests on an UNPROVEN _exhaustive claim (guards are erased in the TLA+ model, so TLC cannot check it); verify the guard set is provably total, or add an unguarded fallback branch so the liveness proof becomes sound
  warn   Shipment.machine.json: counter 'carrierRetries' has an increment and an exhaustion guard but no reset action on any edge; if a re-issued entry is promised a fresh budget, wire the reset on the leg-entry edges, and if one budget per instance is the design, say so with its proof in _counters
  warn   Shipment.machine.json: counter 'retries' has an increment and an exhaustion guard but no reset action on any edge; if a re-issued entry is promised a fresh budget, wire the reset on the leg-entry edges, and if one budget per instance is the design, say so with its proof in _counters
  warn   Shipment.machine.json: liveness for rolledBack rests on an UNPROVEN _exhaustive claim (guards are erased in the TLA+ model, so TLC cannot check it); verify the guard set is provably total, or add an unguarded fallback branch so the liveness proof becomes sound
  warn   FulfillmentSaga.matrix.md:32: `FulfillmentSaga.semantics` names no state, event, or unit of FulfillmentSaga; the narrative has rotted or the name moved
  note   Order.machine.json: state rolledBack always-list is fully guarded; liveness rests on the declared exhaustiveness: priorStatus is set by every setPending* action to the current domain state; only Pending, Confirmed, Reserved, Paid, and Shipped have transitions into the persist overlay (final states persist nothing), and all five priorIs* guards are present
  note   OutboxMessage.machine.json: state rolledBack always-list is fully guarded; liveness rests on the declared exhaustiveness: priorStatus is set on every path into the overlay from a domain state; only Pending and Published reach the overlay (Consumed is final), and both priorIs* guards are present; a rollback to Pending is precisely the at-least-once re-drive point for the next poller sweep
  note   Payment.machine.json: state gatewayResume always-list is fully guarded; liveness rests on the declared exhaustiveness: gatewayOp is set by setGatewayAuthorize/setGatewayCapture/setGatewayRefund before entering the three gateway-call states, the only states that reach gatewayRetry; the three gatewayFor* guards cover {authorize, capture, refund} totally
  note   Payment.machine.json: state rolledBack always-list is fully guarded; liveness rests on the declared exhaustiveness: priorStatus is set on every path into the overlay from a domain state; only Pending, Authorized, and Captured reach the overlay (Failed and Refunded are final), and all three priorIs* guards are present
  note   Reservation.machine.json: state rolledBack always-list is fully guarded; liveness rests on the declared exhaustiveness: only Held has transitions into the persist overlay (Committed and Released are final), so priorStatus is always Held and the single priorIsHeld guard is total
  note   Shipment.machine.json: state rolledBack always-list is fully guarded; liveness rests on the declared exhaustiveness: priorStatus is set on every path into the overlay from a domain state; only Pending, Dispatched, and InTransit reach the overlay (Delivered and Lost are final), and all three priorIs* guards are present
  checked: 6 machines, 138 transitions, 6 tla-generable, 6 oracles fresh, 168 named units covered, 6 states relying on unproven _exhaustive liveness, 1 dotted references checked
== Gd-idcite  design-side stable-id citations ==
  warn   BUILD.md:123: T-ORDE-01 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:124: T-ORDE-01 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:129: T-PAYM-01 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:134: T-FULF-02 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:136: T-OUTB-01 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:137: T-FULF-08 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:140: T-FULF-05 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:141: T-FULF-14 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:159: T-FULF-02 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:160: T-ORDE-01 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:160: T-ORDE-16 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:161: T-OUTB-01 is a positional row citation; rows renumber, so cite the stable id instead
  warn   BUILD.md:161: T-OUTB-02 is a positional row citation; rows renumber, so cite the stable id instead
  checked: 3 citations resolved, 20 files scanned, 138 committed oracle ids
== Gl-ledger  session ledgers + house style ==
  checked: 20 files style-scanned
  ok
== Gx-trace  cross-layer traceability ==
  warn   invariant 'address-country-present' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'line-item-quantity-positive' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'order-delivered-terminal' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'product-price-nonneg' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'refund-amount-positive' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'reservation-quantity-positive' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'reserve-before-pay' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  warn   invariant 'saga-terminal' is carried by preserves but realized by no machine unit or relational layer; its enforcement rests on the prose tables and the tests they name (map it in a machine matrix or a relational layer)
  note   FulfillmentSaga: 1 operational-overlay states (compensateRetry)
  note   Order: 3 operational-overlay states (persisting, persistRetry, rolledBack)
  note   OutboxMessage: 4 operational-overlay states (publishing, persisting, persistRetry, rolledBack)
  note   Payment: 8 operational-overlay states (authorizing, capturing, refunding, gatewayRetry, gatewayResume, persisting, persistRetry, rolledBack)
  note   Reservation: 3 operational-overlay states (persisting, persistRetry, rolledBack)
  note   Shipment: 5 operational-overlay states (dispatching, carrierRetry, persisting, persistRetry, rolledBack)
  checked: 12 entities, 6 lifecycle machines traced, 6 lifecycle entities with machines, 3 placement rows with machines, 4 placement rows waived, 1 entities placement-waived, 11 entities placed, 4 event-contract reactions waived, 5 event-contract emissions traced, 4 event-contract reactions traced, 3 event-contract emissions waived, 13 event-contract consumer reads declared, 22 declared read fields carried, 1 build mode declared, 1 toolchain section present, 1 state-migration section present, 8 invariants attested only (prose), 17 invariants enforced, 16 invariants unit-backed (guard/action/actor), 1 invariants integrity-checked (relational model)
== Gb-plan  build plan structure ==
  checked: 1 plans, 6 milestones, 6 DoD-bearing milestones, 1 skeleton NFR lines, 8 skeleton citations
  ok

0 blocking (ERROR/DRIFT) finding(s)
design-green: all applicable design gates green (add --impl for G4-import and Gt-tests toward platform-green)
